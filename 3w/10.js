//Write a JavaScript program to extract out the values at the specified indexes from an specified array. 

const fun = (arr, removeIndexes) => {
  let removed = []; 
  let pull = arr.map(i => )
}

console.log(fun([1, 2, 4, 6, 5 ,9, 0], [2, 3, 4])); 