//Write a JavaScript function to convert an amount to coins.

const coins = (x, cents) => {
  if (x === 0) {
    return [];
  }
  if (x >= coins[0]){
    re
  }
};

console.log(coins(46, [25, 10, 5, 2, 1]));
